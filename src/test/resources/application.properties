# FirebaseAuthService is mocked in tests — skip SDK initialisation at startup.
quarkus.arc.exclude-types=com.grandtech.config.FirebaseConfig

# Tests connect to the AuraDB instance configured in application.properties.
# DevServices (Docker-based Neo4j) is disabled to avoid pulling a container image.
quarkus.neo4j.devservices.enabled=false
